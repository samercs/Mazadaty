@using Mzayad.Core.Formatting
@model DateTime?

@if (!Model.HasValue)
{
    <span class="text-warning">Never Subscribed</span>
}
else
{
    if (Model.Value > DateTime.UtcNow)
    {
        <span>
            <span class="text-success">ACTIVE </span> until @DateTimeFormatter.ToLocalTime(Model.Value)
        </span>
    }
    else
    {
        <span>
            <span class="text-danger">EXPIRED </span> on @DateTimeFormatter.ToLocalTime(Model)
        </span>
    }
}
